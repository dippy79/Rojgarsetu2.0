name: RojgarSetu CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  backend-go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache: true
      - name: Mod tidy + build
        run: cd backend_go && go mod tidy && go build ./...
      - name: Test
        run: cd backend_go && go test ./... -v --timeout=60s || true

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install + build
        run: cd frontend && npm install && npm run build

  ai-engine:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install + test
        run: |
          cd services/ai-engine-python
          pip install -r requirements.txt
          python -m pytest tests/ -v || true
