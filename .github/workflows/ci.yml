name: RojgarSetu CI/CD

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master ]
  schedule:
    - cron: '0 2 * * 0'
  workflow_dispatch:
# GitHub Packages (GHCR) mein push karne ki permissions
permissions:
  contents: read
  packages: write
env:
  GO_VERSION: '1.23'
  NODE_VERSION: '20'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Go lint backend
        uses: golangci/golangci-lint-action@v6
        with:
          working-directory: backend_go

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go ${{ env.GO_VERSION }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Go test backend
        working-directory: backend_go
        run: go test ./...

  build:
    needs: [lint, test]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # GHCR ke liye repository name ko lowercase kar rahe hain
      - name: Lowercase repo name
        id: lowercase
        run: |
          echo "repo_lc=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT

      - name: Docker login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push backend
        uses: docker/build-push-action@v6
        with:
          context: backend_go
          file: backend_go/Dockerfile
          push: true
          tags: ghcr.io/${{ steps.lowercase.outputs.repo_lc }}/backend:latest

      - name: Build & push frontend
        uses: docker/build-push-action@v6
        with:
          context: frontend
          file: frontend/Dockerfile
          push: true
          tags: ghcr.io/${{ steps.lowercase.outputs.repo_lc }}/frontend:latest